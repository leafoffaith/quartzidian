* `useRef` => Most use cases involve targeting a specific DOM element as the render target. Referring directly to the DOM. But it is just a container
	* only 1 property is exposed, current.
	* keep track of the same thing that needs to be === to something
---


* `useReducer` => can be used as a replacement for useState. true/false things can be done with useState since it is just easier.
* `memo` => memo guarantees that something does not re-render. 
* `useCallback` => 