If CORS needs to be enabled in a React app, have to set it up in the server. It should always be handled server side. 
In the case of metadata app, it is being handled by Terraform that creates the endpoint for me to work with
So, the endpoint hits the 