
Why return React.Fragment? <><</>


Ternary expressions react
Short circuit evaluation with &&
```
<>
     {error && <h1>Error</h1>}
     {!error ? <h1>Success</h1> : null}
   </>
```

```
function App() {
  const error = true;
  const type = 'text';
  return (
    //props
    <>
      <label htmlFor="input">Input: </label>
      <input id="input" type={type} maxLength="3"></input>
    </>
  );
}
```

```
function App() {
  const error = true;
  const props = {
    id: 'input',
    type: 'text',
    maxLength: '3'
  }
  return (
    //props
    <>
      <label htmlFor="input">Input: </label>
      <input {...props}></input>
    </>
  );
}
```

```
  return (
    //props
    <>
      <label htmlFor="input">Input: </label>
      <input {...props} placeholder="user" />
    </>
  );
}
```

```
function App() {
 
  return <p style={{
    color: 'red',
    textAlign: 'center',
    fontSize: '48px'
  }} className="center">Hello World</p>
}
```

----
## Props

```

function App() {
  return (
    <>
      <Hello name="Nico" />
      <Hello name="Nico" />
      <Hello />
    </>
    
  );


function Hello(props){
	return <h1>Hello {props.name}</>
}
  function Hello({name = 'User'}){
    return <h1>Hello {name}</h1>
  }
}
```