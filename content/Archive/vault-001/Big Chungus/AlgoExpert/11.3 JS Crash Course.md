```
const promise = new Promise((resolve, reject) => {
	//will change the value to 2 and fulfill the request
	 resolve(2);
	//will reject 
	reject(2);
})

console.log(promise);

//waits for the promise to settle and then passes in the value
%%promise.then(value => console.log(value), error => console.log('Oh no ' + error))%%

//console.log is a function itself, it will automatically log value //here
promise.then(console.log, error => console.log('Oh no ' + error))

promise.then(console.log).catch()
error => console.log('oh no ' + error)

~then function returns itself a promise, it will take whatever is in the callback and return a new promise with that promise 

~catch works the same way but returns a rejected promise

const promise2 = Promise.resolve(3);
//will return a resolved promise

promise
.then(value => value * 2)
.then(value => value + 1)
.then(value => {
	//will be caught by catch statement
	throw new Error('Something went wrong')
})
.then(console.log)
.catch(error => console.log('Oh No' + error))
//finally will always run no matter what
.finally(() => console.log('Done'))



```

```
//async will implicitly return a promise
async function tester() {
	//await waits for promise to settle
	try{
		await new Promise((res, req) => setTimeout(() => res(3), 1000))
	console.log(value)
	} catch (error) {
		console.log('Error: ' + error)
	}
	
} 
tester();

OR 

tester().then(console.log).catch(error => console.log('Oh no '+ error))
//different from then cause of the syntax


```