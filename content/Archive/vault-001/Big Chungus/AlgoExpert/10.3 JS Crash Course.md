```
// Welcome to our Playground!

// const arr = [1, 2, 3, 4].map(num => num * 2);
const arr = [1, 2 , 3, 4]

function add(x, y) {
  console.log(x + y)
}


//Here if the array is spread and the function only 
//accepts two values then first two will be passed
add(...arr);

// console.log(arr);

const person = {
  name: "Nico",
  age: "5"
}

const person2 = {
  name: "Rachel",
  age: "50"
}

// function printName({name}){
//   console.log(name)
// }

// function printName2(person){
//   // console.log(person.name)
//   console.log(person)
// }

const printName = ({name}) => {
  console.log(name)
}


printName(person);
// printName2(person2);



const shouldRunCode = true;

//Use this to optionally change while reading from an API
console.log(person?.company?.website ?? 'Foo')

shouldRunCode && console.log(`Hello World`)
```

---
## Promises

```

```
