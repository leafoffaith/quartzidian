What is an <mark class="hltr-yellow">abstract class</mark>
	Can't instantiate 

You can choose to have abstract methods 
	Non-implemented method 
Implement it in child classes using <mark class="hltr-pink">@override</mark>

What's the difference between abstract & interface
	Every method is assumed to be abstract
	Implements instead of extend
	You can implement as many interfaces but extend only one abstract
	All fields declared inside an interface will be <mark class="hltr-cyan">static</mark> and <mark class="hltr-cyan">final</mark>

