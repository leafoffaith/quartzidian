why main() method? JRE calls on the main method 

What is a method signature? (public static void)

public anyone can call it, private means only the class can call it

static, means that the method can be called without needing an instance of it to call it
That's why in factory classes, you need to allow getInstance to be public static to instantiate a new object of the factory class 
