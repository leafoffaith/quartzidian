**DNS**
Domain Name Service 

Was created to use UDP at first 

Well Known Ports (WKP)
The well-known port number for DNS is **53**, and that's where the server process should be listening for client requests.

When a computer makes a request it will include the transport layer protocol + the DNS port + a source port that's a random number over 1024

Included IP address of the server while making the request
![[Pasted image 20230106095422.png]]
MAC is hard-coded into the ethernet adapter

Application layer
![[Pasted image 20230106095409.png]]


**WEB-SERVICES**
The protocol used is called *HTTP (Hyper Text Transfer Protocol)*

![[Pasted image 20230106095612.png]]

![[Pasted image 20230106102749.png]]

